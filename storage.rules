rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // そういうことよ
    match /{allPaths=**} {
      allow read, write if request.auth != null && request.auth.admin == true;
    }
  }
}
